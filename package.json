{
  "name": "vedha-json-mcp",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx server/src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node server/dist/index.js",
    "test": "node --import tsx --test server/test/*.test.ts && node --test scripts/test/*.test.mjs",
    "validate:review": "node scripts/validate-review.mjs",
    "render": "node scripts/render-report.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
